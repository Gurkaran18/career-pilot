import React from "react";

export default function TemplateGallery() {
  return (
    <div className="min-h-screen bg-background text-foreground p-8 pt-24">
      <h1 className="text-4xl font-bold">Template Gallery (placeholder)</h1>
      <p className="text-sm text-muted-foreground mt-4">The original TemplateGallery.jsx was corrupted. This placeholder prevents parse errors so the app can run. Restore the full implementation from TemplateGallery.jsx.bak when ready.</p>
    </div>
  );
}
